[package]
name = "led-blinker"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = "s"
lto = true # Enable Link Time Optimization for better performance and smaller binary size
panic = "abort"
